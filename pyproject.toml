[project]
name = "wildlife-classifier"
version = "0.1.0"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["wildlife_classifier"]

[tool.pytest.ini_options]
pythonpath = ["."]
